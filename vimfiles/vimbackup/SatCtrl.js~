'use strict';

angular.module('sat.controllers')
.controller('SatCtrl', function ($scope) {
    $scope.user = {
        firstName: "test",
        lastName: "test"
    };

    $scope.logins = [];

    $scope.login= function(){
        $scope.logins.push($scope.user.lastName);
    };
});
