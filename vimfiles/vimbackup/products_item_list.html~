{% load staticfiles %}
<div id="marketing_row" class="row-fluid">
    {% for product in products %}
    <div class="marketing">
        <div class="span4">
            <hr>
            <h2>{{ product.name }}</h2> 
            <p> {{ product.short }}</p>
            {% for image in product.images.all %}
            <img src="{% static image.image.url %}" height="150px" width="150px">
            {% endfor %}
            <p><a class="btn" id="{{ product.id }}" href="#">View details »</a></p>
        </div>
    </div>
    {% endfor %}
</div>

