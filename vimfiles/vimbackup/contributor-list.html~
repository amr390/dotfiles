<div id="container-contributor-list">
<!-- CONTRIBUTOR FIELDS -->
    <h2>Contribuyentes</h2>
    <table class="table table-hover table-responsive">
        <thead>
            <tr>
                <th>id</th>
                <th>DNI/CIF</th>
                <th>Nombre</th>
                <th>Apellidos</th>
                <th>E-mail</th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="contributor in contributors">
                <td>{{ contributor.id }}</td>
                <td>{{ contributor.identifier }}</td>
                <td>{{ contributor.first_name}}</td>
                <td>{{ contributor.last_name }}</td>
                <td>{{ contributor.email }}</td>
                <td><a ng-click="remove(contributor.id)"><span class="glyphicon glyphicon-remove" ></span> </a></td>
                <td><a ng-click="edit(contributor.id)"><span class="glyphicon glyphicon-pencil" ></span> </a></td>
            </tr>
        </tbody>
    </table>
    <a href="#/contributor/new" class="btn btn-success pull-right">Nuevo</a>
</div>
<!-- CONTRIBUTOR FIELDS -->
