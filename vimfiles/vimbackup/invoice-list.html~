<div class="container payable">
    <h2>Facturas devengadas</h2>
    <table class="table table-hover table-responsive">
        <thead>
            <tr>
                <th>Id</th>
                <th>Número Factura</th>
                <th>Concepto</th>
                <th>Contribuyente</th>
                <th>Fecha efectiva</th>
                <th>Fecha inclusión</th>
                <th>Base</th>
                <th>Iva</th>
                <th>Irpf</th>
                <th>Total</th>
                <th>Borrar</th>
                <th>Editar</th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="invoice in invoices">
                <td>{{ invoice.id }}</td>
                <td>{{ invoice.invoice_number }}</td>
                <td>{{ invoice.concept }}</td>
                <td>{{ invoice.contributor }}</td>
                <td>{{ invoice.date_issue }}</td>
                <td>{{ invoice.date_listing }}</td>
                <td>{{ invoice.base_cost }}</td>
                <td>{{ invoice.vat_cost }}</td>
                <td>{{ invoice.tax_cost }}</td>
                <td>{{ invoice.total_cost }}</td>
                <td style="text-align:center"><a ng-click="remove(invoice.id)"><span class="glyphicon glyphicon-remove" ></span> </a></td>
                <td style="text-align:center"><a ng-click="edit(invoice.id)"><span class="glyphicon glyphicon-pencil" ></span> </a><td>
            </tr>
        </tbody>
    </table>
    <a href="#/invoice/income/new" class="btn btn-success pull-right">Nuevo</a>
</div>
