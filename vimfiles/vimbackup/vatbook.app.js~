(function(){
    /*jshint unused: vars */
    'use strict';

    angular.module('vatbook.services', []);
    angular.module('vatbook.controllers', ['vatbook.services']);
    angular.module('vatbook.directives', []);

    var vatbook = angular.module('vatbook.app', [
        'vatbook.controllers' ,
        'vatbook.services', /*angJSDeps*/ //'ngCookies'
        'ngResource',//, 'ngSanitize',
        'ngRoute'
        //'ngCookies'
    ]);

    vatbook.config(['$resourceProvider', function($resourceProvider){
        // Don't strip trailing slashes from calculated URLs
        $resourceProvider.defaults.stripTrailingSlashes = false;
    }]);

}());
