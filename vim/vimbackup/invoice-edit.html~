<div>
  <h2>Factura Deducible</h2>
  <form role="form">
    <div class="form-group">
      <label for="tax_contributor">Contribuyente:</label>
      <select ng-model="invoice.contributor" class="form-control" id="tax_contributor">
          <option ng-repeat="contributor in contributors" value="{{ contributor.id }}">{{ contributor.first_name }} {{ contributor.last_name }} </option>
      </select>
    </div>
    <div class="form-group">
      <label for="tax_concept">Concepto:</label>
      <select ng-model="invoice.concept" class="form-control" id="tax_concept">
          <option ng-repeat="concept in concepts" value="{{ concept.id }}"> {{ concept.name }}</option>
      </select>
    </div>
    <div class="form-group">
      <label for="tax_client">Cliente:</label>
      <select ng-model="invoice.client" type="text" class="form-control" id="tax_client" placeholder="select a client">
          <option ng-repeat="client in clients" value="{{ client.id }}"> {{ client.full_name }}</option>
      </select>
    </div>
    <div class="form-group">
        <label for="tax_base_cost">Base:</label>
        <input ng-model="invoice.base_cost" type="text" class="form-control" id="tax_base_cost" placeholder="Base cost">
    </div>
    <div class="form-group">
        <label for="tax_vat_cost">IVA:</label>
        <input ng-model="tax.vat_cost" type="text" class="form-control" id="vat_cost" placeholder="VAT cost based on concept">
    </div>
    <div class="form-group">
        <label for="tax_percent_applied">%IVA Aplicado:</label>
        <input ng-model="tax.percent_applied" type="text" class="form-control" id="tax_percent_applied" placeholder="% in tax dutie">
    </div>
    <div class="form-group">
        <label for="tax_effective_cost">IVA Aplicado:</label>
        <input ng-model="tax.effective_cost" type="text" class="form-control" id="tax_effective_cost" placeholder="Effective VAT cost">
    </div>
    <div class="form-group">
        <label for="tax_date_issue">Fecha Efectiva:</label>
        <input ng-model="invoice.date_issue" type="date" class="form-control" id="tax_date_issue" placeholder="Select date issue">
    </div>
    <div class="form-group">
        <label for="tax_date_listing">Fecha Registro:</label>
        <input ng-model="invoice.date_listing" type="date" class="form-control" id="tax_date_listing" placeholder="Select date listing">
    </div>
    <div class="btn-group pull-right">
            <a class="btn btn-danger" href="#/invoice/tax/list">cancelar</a> 
            <a class="btn btn-success" ng-click="save()">Salvar</a>
    </div>

  </form>
</div>
